<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "つぶやきましょう..." %>
  </div>
  <%= f.submit "つぶやく", class: "btn btn-primary" %>
<% end %>
